<br>
<div style="background: #b3ffb3 !important" class="jumbotron">
  <h1>Blocipedia</h1>
    <p>Create and share Markdown Wikis</p>
      <div>
        <% if user_signed_in? %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path, :class => 'btn btn-info btn-large' %>
        <%end%>
      </div>
</div>
<div class="container">
  <div class="rows">
    <div class="col-md-8">
      <% if current_user %>
    
      <% else %>
        <h2>Create as many public wikis as you want!</h2>
          <%= link_to "Sign Up for a Free Plan", new_user_registration_path, :class => 'btn btn-info btn-large' %>
      <% end %>
    </div>
      
  <div class="col-md-4">
    <h2>Create private wikis!</h2>
      <% if user_signed_in?  && current_user.premium? %>
    <h6> You are currently a premium member!</h6>
      <% elsif current_user && current_user.standard? %>
        <%= link_to "Upgrade to a Premium Plan",new_charge_path, :class => 'btn btn-info btn-large' %>
      <% else %>
        <h6>Create a free plan and upgrade to premium!</h6>
      <% end %>
      
    </div>
  </div>
</div>